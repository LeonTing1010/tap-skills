## What

<!-- site/name — one sentence -->

## Strategy

<!-- API / DOM / SSR / macOS — why this approach? -->

## Test output

<!-- Paste `tap <site> <name>` output (first few rows) -->

```
$ tap site name
```

## Checklist

- [ ] `node test/tap-format.test.mjs` passes
- [ ] Tested live with `tap <site> <name>`
- [ ] `site` matches directory, `name` matches filename
- [ ] API-first where possible
